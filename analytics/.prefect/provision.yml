profiles:
  dev:
    work_pools:
      process_pool:
        type: process
        concurrency_limit: 1
    work_queues: null

  prod:
    work_pools:
      process_pool:
        type: process
        concurrency_limit: 1
    work_queues: null
