services:
  prefect-base:
    requirements:
      - ./requirements.txt
      - ./requirements_dev.txt
  jupyter:
    requirements:
      - ./requirements.txt
  api:
    requirements:
      - ./requirements.txt
      - ./requirements_api.txt
